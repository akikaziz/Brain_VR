using System.Collections;
using System.Collections.Generic;
using UnityEngine;


//This is the invisible pointer on the controller that will cast to a brain part to update the info panel
public class RayCastSelector : MonoBehaviour
{
    //function to get the collider of the raycast target from this object

    //check to see if the hit is on a gameojbect that's tagged with a brain part

    //if it is part of the brain, call the show info panel function on that game object returned from the ray cast
}
